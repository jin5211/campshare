<!DOCTYPE html>
<html>
  <head>
    <title>CampShare</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "actiontext", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  </head>

  <body>
    <header class="header navbar-expand-lg navbar-light bg-dark py-3 sticky-top">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to "CampShare" , root_path , class: :logo %>
          </div>
          <% if user_signed_in? %>
          <div class="nav__right">
            <%= link_to "ログアウト", destroy_user_session_path, class: :nav__logout , data: { turbo_method: :delete }%>
            <%= link_to "NewBlog", new_blog_path, class: :nav__btn %>
          </div>
          <% else %>
          <div class="nav__right">
            <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
            <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
          </div>
          <% end %>
        </div>
      </div>
    </header>
    <div class="container mt-5 mb-5 ">
      <%= yield %><link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    </div>
    <footer class="footer fixed-bottom">
      <p class="copyright">Copyright © CampShare All rights reserved.</p>
    </footer>
  </body>
</html>
